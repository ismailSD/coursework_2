var assert = require('assert');
describe('node hello world console app', function() {
  describe('#indexOf()', function() {
    it('It should log hello world with port and hostname', function() {
      assert.equal("Hello world");
    });
  });
});
